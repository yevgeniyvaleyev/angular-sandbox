import { SkunsikDirective } from './skunsik.directive';

xdescribe('SkunsikDirective', () => {
  it('should create an instance', () => {
    // const directive = new SkunsikDirective({
    //   format: 'uppercase'
    // });
    // expect(directive).toBeTruthy();
  });
});
